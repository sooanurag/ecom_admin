import 'package:flutter/material.dart';

class EcomAdmin extends StatelessWidget {
  const EcomAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}